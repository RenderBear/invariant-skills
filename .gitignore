# Packaged skill bundles (build artifacts)
*.skill
dist/

# Local agent state — installs are symlinks, not repo content
.claude/
.cursor/
.codex/
!AGENTS.md

# Editors
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# NOTE: .intent/ is deliberately NOT ignored. Accepted domains and contracts,
# architecture pointers, discoveries, and audits are reviewed repository state.
# `.intent/runtime/` creates its own ignore marker because it is local planning
# state shared by linked worktrees and must also work before tracked intent exists.
